<p id="notice"><%= notice %></p>
<% if !user_signed_in? %>
               <strong> <%= link_to "Log in", new_user_session_path %></strong>
              <% else %>
                <strong><%= link_to "Выйти", destroy_user_session_path, method: :delete %></strong>
                <% end %>

<h1>Услуги строительной компании:</h1>
 
 
<table>
  <thead>
    <tr>
      <th>Название</th>
     <!--  <th>Описание</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @adminkas.each do |adminka| %>
      <tr>
        <td><%= adminka.title %></td>
        <!-- <td><%#= adminka.body %></td> -->
        <td><%= link_to 'Показать', adminka %></td>
        <td><%= link_to 'Редактировать', edit_adminka_path(adminka) %></td>
        <td><%= link_to 'Удалить', adminka, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Добавить услугу', new_adminka_path %>
